# 「小食叙记」项目交接总结

## 1. 项目定位与架构

本项目是校园食堂信息、打卡与社区互动小程序。

- 后端：Flask + SQLite，原有网页端路由仍保留。
- 小程序：原生微信小程序，目录为 `miniprogram/`，使用自定义底部导航。
- 小程序 API：`api_routes.py`，统一前缀为 `/api`，使用 Bearer Token。
- 图片：上传到 Flask 静态目录，单张上限 5MB。
- 小程序默认 API 地址：`miniprogram/app.js` 中的 `http://127.0.0.1:5000/api`，适用于微信开发者工具本机调试。

## 2. 已完成模块

### 用户与权限

- 小程序已接入微信登录入口；本地开发环境可使用后端的开发回退逻辑。
- 用户信息、打卡、实拍投稿、上新情报、收到的点赞与评论均在“我的”页展示。
- 管理员工作台仅在 `isAdmin` 用户的“我的”页显示，后端管理员接口也有权限校验。

### 首页与食堂

- 首页包含：手绘食堂地图、内嵌菜品搜索、上新情报入口、今日推荐、特价菜品、食堂一览及人流状态点。
- “今日推荐”最多展示 4 道高评分菜品。
- “特价菜品”最多展示 4 道；没有特价时仍显示占位提示。
- 食堂详情显示档口；点击档口在当前页面展开菜品，而非额外跳转。
- 菜品详情支持评分与实拍图投稿入口；菜品评论按当前产品决定已移除。

### 打卡与中间入口

- 三餐打卡使用月历和温暖确认弹窗。
- 打卡来源可选“食堂 / 外卖 / 聚餐 / 自定义”。
  - 食堂：模糊搜索后选菜品。
  - 其他来源：填写自由文本，后端会记录来源前缀。
- 打卡页“拍张照片”会直达实拍图上传模式。
- 中间入口支持实拍上传和按早餐、午餐、晚餐随机推荐菜品（骰子）。
- 上传图片支持拍照或相册选择，客户端会尝试压缩至 5MB 内，后端再次校验。

### 社区

- 社区为浏览优先的帖子流，右下角悬浮加号发布。
- 帖子支持文字与最多 3 张图片；发布选择“拍照 / 从相册选择”，超过 5MB 自动压缩。
- 帖子包含昵称、发布时间、点赞数、评论数、封面缩略图。
- 支持帖子点赞、评论、回复评论；从“我的”通知进入帖子后会高亮对应评论。
- 当前列表页只显示帖子第一张图片，详情页显示全部图片。

### 管理员工作台

- 搜索菜品并修改原价、设置或撤销特价。
- 已设置特价的菜品常驻显示在“今日特价”区域，可直接撤销。
- 审核用户实拍图；审核通过后可设为菜品主图，并奖励投稿积分。
- 审核上新情报。
- 删除帖子及菜品/社区评论。
- 查看并处理用户来信。

### 用户来信

- “我的”页新增「和饭搭子说说」入口，替代生硬的“意见反馈”。
- 用户可提交“建议 / 问题 / 想说的话”。
- 管理员工作台显示待处理来信，并可标记“已处理”。

## 3. 主要文件

- `app.py`：Flask 应用工厂；对 `/api/` 放行 CSRF，注册 API 路由。
- `api_routes.py`：小程序 API、认证、社区、打卡、上传、管理端接口。
- `schema.sql`：完整数据库初始化结构。
- `miniprogram/app.js`：小程序 API 地址、请求封装、上传封装、微信登录。
- `miniprogram/custom-tab-bar/`：自定义底部导航。
- `miniprogram/pages/home/`：首页。
- `miniprogram/pages/checkin/`：打卡。
- `miniprogram/pages/community/`、`miniprogram/pages/post/`：社区与详情。
- `miniprogram/pages/action/`：上传实拍图与随机菜品。
- `miniprogram/pages/admin/`：管理员工作台。
- `miniprogram/pages/me/`：个人中心与“和饭搭子说说”。

## 4. 数据库兼容与运行注意

- `api_routes.py` 启动时会通过 `CREATE TABLE IF NOT EXISTS` 自动补齐 API 相关表，包括：`api_token`、`new_dish_report`、`dish_image_submission`、`wechat_identity`、`user_feedback`。
- 旧数据库的 `post_comment` 会自动补齐 `parent_id` 字段。
- 修改后端后，若 Flask 未运行在自动重载模式，需要重启后端；微信开发者工具需重新编译小程序。
- 若只用微信开发者工具调试，可保留 `127.0.0.1` API 地址；真机调试、体验版和上线必须改为 HTTPS 合法域名，不能使用 localhost。
- 内容安全配置：生产服务器设置 `WECHAT_APPID`、`WECHAT_APPSECRET`、`WECHAT_CONTENT_SAFETY=1`；建议同时设置 `WECHAT_CONTENT_SAFETY_FAIL_CLOSED=1`，以便微信审核服务不可用时拒绝发布而非放行。
- 食堂数据已迁移为“建筑 → 楼层/餐厅”：`canteen_building` 保存建筑与坐标，`canteen.building_id` 关联具体餐厅。旧数据会在后端启动时自动归类。

## 5. 已知不完善项与后续建议

### 已确认的核心问题（本轮补充）

1. **地图不真实**：首页地图为静态手绘示意，不能准确表达校园道路、食堂入口、楼层或当前位置。上线前应换成校内真实平面图，或接入腾讯位置服务；地图上需要明确标注“示意图”或“实时位置”。
2. **人流状态**：已改为“同学主动报送”。进入食堂会询问“你现在在这个食堂吗？人多不多？”，用户选择空闲/适中/拥挤后，后端仅汇总最近 90 分钟报送并同步首页；每位用户每天可更新一次。长期仍可接入闸机、摄像头计数或匿名用户主动上报，并保存更细的数据来源。
3. **社区和评论内容安全**：已增加服务端自动规则拦截，覆盖敏感词、多个外部链接、长串重复字符，并应用于帖子、评论、上新情报、实拍菜名和用户来信。该规则不是语义 AI 审核；上线前仍需接入微信内容安全能力、举报、屏蔽、封禁与审核日志。
4. **实拍图没有多图管理**：目前一个菜品只会使用一张审核通过的主图，无法查看全部实拍、切换主图、删除违规图或让用户浏览图片墙。需要建立菜品图片列表、主图标记、排序、审核状态、删除/举报和管理员批量管理能力。

### P0：上线前必须完成

1. 配置真实微信登录。
   - 在服务端配置 `WECHAT_APPID`、`WECHAT_APPSECRET`。
   - 配置管理员 OpenID 白名单 `WECHAT_ADMIN_OPENIDS`。
   - 移除或限制当前开发环境的登录回退逻辑，避免任何开发账号获得管理员能力。
2. 部署 HTTPS 后端并在微信公众平台配置合法 request/upload/download 域名。
3. 迁移 SQLite 到受控生产数据库，加入备份、日志、错误监控和上传文件清理策略。
4. 对管理员账号、上传文件、内容审核、限流和异常输入做安全测试。

### P1：产品核心能力补全

1. “拍照扫描卡路里”尚未实现；现在是上传实拍图，不包含图片识别、菜品识别或热量计算。
2. 地图仍为演示能力；人流已改为近 90 分钟同学报送，详见上方“已确认的核心问题”。
3. 上新情报审核后目前只更新审核状态，不会自动创建“食堂 / 档口 / 菜品”；管理员需要补充录入流程。
4. 实拍图多图管理尚未实现，详见上方“已确认的核心问题”。
5. 特价菜没有生效时间、库存、售罄状态或到期自动撤销能力。
6. 评分没有防刷、评分理由、评分分布或管理员处理入口。

### P2：体验与内容治理优化

1. 社区列表目前只显示首张图片；可升级为单图/双图/三图九宫格布局，避免多图信息丢失。
2. 社区点赞目前只更新数量，未表现“我已点赞”的状态；可补充取消点赞和按钮状态。
3. 回复结构目前为单层父评论引用；如需要真正的多层会话，应补充评论树、折叠和通知规则。
4. “和饭搭子说说”仅支持管理员标记已处理，不支持管理员回复、用户查看处理状态或消息通知。
5. 首页、食堂、人流、菜品照片中仍有演示数据和 AI 图，需要逐步替换为实拍、人工维护的数据。
6. 需要在真实设备上做全面 UI 回归，尤其是不同安卓机型的 emoji 字形、小程序安全区和图片压缩表现。

## 6. 验证记录

- 已完成 Python AST 语法检查：`app.py`、`api_routes.py`。
- 已完成小程序 JavaScript 语法检查：`miniprogram/` 下全部 `.js` 文件。
- 已验证 `user_feedback` 表可由应用启动自动创建。
- 尚未进行真机微信登录、真实 HTTPS 域名、并发上传、完整管理员审核闭环测试。

## 7. 当前 Git 状态

当前改动尚未提交：

- 已修改：`app.py`、`schema.sql`。
- 新增未跟踪：`api_routes.py`、整个 `miniprogram/` 目录。

交接前建议：在微信开发者工具完成一次完整手工验收后，将本轮改动拆分为“后端 API / 数据库”和“小程序 UI / 页面”两个提交，方便回滚与审查。
