# 小食叙记 — 北京交通大学食堂信息平台

## 项目概述

"小食叙记"是一个面向北京交通大学师生的食堂信息共享平台，提供菜品浏览、评分评论、社区交流和三餐打卡等一站式服务。项目采用轻量级技术栈，已上线运行。

**上线地址**：http://wangyouran.pythonanywhere.com

## 技术架构

| 层级 | 技术选型 |
|------|----------|
| 前端 | HTML + CSS + JavaScript + Bootstrap 5 (CDN) |
| 后端 | Python Flask（模块化蓝图路由） |
| 数据库 | SQLite（10 张表） |
| 认证 | Flask-Login + bcrypt 密码加密 |
| 安全 | CSRF Token 防护、session 管理 |
| 部署 | PythonAnywhere 免费版 (WSGI) |

整体采用服务端渲染（SSR）架构，Flask Jinja2 模板直接渲染 HTML，前端仅处理少量交互逻辑（搜索联想、图片预览等），不引入前端框架，保持轻量和低依赖。

## 数据规模

数据库覆盖北京交通大学 **8 个食堂**、**63 个档口**、**1,400 道菜品**：

| 食堂 | 楼层 | 菜品数 | 代表档口 |
|------|:---:|:------:|----------|
| 东区食堂 | 一层 | 292 | 交大小笼包、重庆小面、炸鸡汉堡、铁板意面、江西瓦罐汤 |
| 学活食堂 | 一层 | 93 | 知味主食厨房、基本伙窗口 |
| 学活食堂 | 二层 | 53 | 风味档口 |
| 学活食堂 | 三层 | 558 | 各类风味档口 |
| 学生四食堂 | 一层 | 90 | 减脂餐窗口 |
| 明湖食堂 | 一层 | 65 | 基本伙、面食 |
| 明湖食堂 | 二层 | 174 | 风味档口 |
| 西区食堂 | 一层 | 75 | 基本伙窗口 |

## 项目结构

```
小食叙记/
├── app.py                    # Flask 应用工厂（CSRF、错误处理、注册模块）
├── wsgi.py                   # PythonAnywhere WSGI 入口
├── requirements.txt          # 依赖：flask, flask-login, bcrypt
├── schema.sql                # 10 张表建表语句
├── init_db.py                # 数据库初始化（建表 + 测试数据）
├── import_real_data.py       # 从 Excel 导入真实菜品数据
├── fill_categories.py        # 根据档口名自动匹配菜品品类
├── check_setup.py            # 部署环境诊断脚本
├── database.db               # SQLite 数据库文件（单文件）
│
├── auth.py                   # 认证模块：注册、登录、登出
├── main_routes.py            # 主路由：首页、搜索、食堂/档口/菜品浏览、评分、评论、API
├── community_routes.py       # 社区模块：发帖、点赞、评论、帖子详情
├── checkin_routes.py         # 打卡模块：三餐打卡、积分、日历、排行榜、个人主页
├── admin_routes.py           # 管理后台：统计、用户管理、内容管理、特价设置
├── helpers.py                # 工具函数：数据库连接、时区处理、图片回退、User 模型
│
├── data/
│   └── 北交食堂菜品收集终版.xlsx  # 完整菜品数据源（1,400 道）
├── static/
│   ├── css/style.css         # 全部样式（响应式布局）
│   ├── js/main.js            # 前端交互（搜索联想、图片预览等）
│   ├── images/
│   │   ├── dishes/           # 16 张 AI 手绘水彩品类图 + category_images.json
│   │   ├── encourage/        # 打卡鼓励图片（可选）
│   │   └── uploads/          # 用户社区发帖上传图片
│   └── uploads/              # 用户上传图片存储
└── templates/                # 12 个 Jinja2 模板
    ├── base.html             # 公共布局（PC 顶部导航 + 移动端底部 TabBar）
    ├── index.html            # 首页
    ├── canteen.html          # 食堂详情页
    ├── stall.html            # 档口详情页
    ├── dish.html             # 菜品详情页（评分 + 评论）
    ├── community.html        # 社区页（热门帖子 + 全部帖子 + 发帖）
    ├── post_detail.html      # 帖子详情页
    ├── checkin.html          # 打卡页（日历 + 排行榜 + 语录）
    ├── profile.html          # 个人主页
    ├── login.html            # 登录页
    ├── register.html         # 注册页
    ├── admin.html            # 管理后台
    └── admin_user.html       # 管理员查看用户详情
```

## 功能模块

### 首页
- **今日推荐**：基于评分×评论数的智能排序算法
- **特价菜品**：管理员动态设置特价，前端原价划线 + 特价高亮对比
- **本周上新**：新品轮播展示
- **热门榜单**：按平均评分排序 TOP 5
- **餐次提示**：根据北京时间（6:30-8:30 早餐 / 11:00-13:00 午餐 / 17:00-19:00 晚餐）自动展示提示语
- **人流量**：基于时段实时计算（拥挤/适中/空闲），精确到分钟

### 食堂 → 档口 → 菜品三级浏览
- 食堂详情页：档口列表、营业时间、实时人流量状态
- 档口详情页：该档口全部菜品，标注新品标签
- 16 个品类配有 AI 生成的手绘水彩风格代表图，含三级回退机制

### 菜品详情
- 基础信息：名称、价格、所属档口、标签、品类
- 五星评分：1-5 分，可修改，一人一票（user_id + dish_id 唯一约束）
- 文字评论：限制 500 字，用户可删除自己的评论
- 评论自动关联时间显示（刚刚 / N 分钟前 / 今天 / 昨天 / N 天前）

### 用户系统
- 注册 / 登录 / 登出，bcrypt 加密存储密码
- 表单验证（用户名 2-20 字符、密码 ≥6 位、两次密码一致）
- 个人主页：积分、评分历史、评论历史、发帖记录、打卡记录
- 管理员标识 + 后台入口

### 社区
- **发帖**：文字（≤1000 字）+ 图片（最多 9 张，单张 ≤5MB）+ 关联菜品
- **菜品搜索联想**：发帖时输入菜名实时模糊匹配
- **热门帖子**：TOP 10 按点赞数排序，与普通帖子去重
- **帖子详情**：点赞（一人一次）、评论（≤300 字）、彩色头像生成、相对时间显示
- **删除**：用户可删除自己的帖子（级联删除点赞和评论）

### 三餐打卡
- 早 / 午 / 晚餐分别打卡，每日每餐限一次
- **积分体系**：早餐 +3 分，午/晚餐 +2 分
- **打卡日历**：月视图，含星期对齐，标注每日打卡次数
- **状态卡片**：全勤表扬 / 半勤鼓励 / 未打卡引导的动态文案
- **积分排行榜**：TOP 10
- **随机语录**：10 条趣味打卡语录

### 管理后台
- **数据统计面板**：用户数、菜品数、帖子数、评分数、评论数、打卡数
- **用户管理**：查看所有用户列表及个人详细活动记录
- **内容管理**：删除违规帖子 / 评论 / 评分
- **特价管理**：搜索定位菜品 → 设置特价 → 首页即时生效

### 搜索
- 支持菜品名称、档口名称、标签的模糊搜索
- 搜索结果分页（每页 20 条）
- API 接口 `/api/dishes/search` 支持前端搜索联想

## 权限设计

| 操作 | 游客 | 用户 | 管理员 |
|------|:---:|:---:|:---:|
| 浏览菜品 / 搜索 | ✓ | ✓ | ✓ |
| 评分 / 评论 | ✗ | ✓ | ✓ |
| 删除自己的评论 | ✗ | ✓ | ✓ |
| 发帖 / 点赞 / 评论帖子 | ✗ | ✓ | ✓ |
| 删除自己的帖子 | ✗ | ✓ | ✓ |
| 三餐打卡 | ✗ | ✓ | ✓ |
| 管理后台 | ✗ | ✗ | ✓ |
| 删除任意内容 | ✗ | ✗ | ✓ |
| 设置特价 | ✗ | ✗ | ✓ |

## 数据库设计

| 表名 | 说明 | 关键字段 | 约束 |
|------|------|----------|------|
| user | 用户 | is_admin, checkin_points, password(bcrypt) | username UNIQUE |
| canteen | 食堂 | name, floor, crowd_status, description | — |
| stall | 档口 | canteen_id, name, location, open_time | FK → canteen |
| dish | 菜品 | stall_id, name, price, special_price, average_rating, category, image_url, is_new, tags | FK → stall |
| rating | 菜品评分 | user_id, dish_id, score | FK → user, dish; UNIQUE(user, dish) |
| comment | 菜品评论 | user_id, dish_id, content | FK → user, dish |
| post | 社区帖子 | user_id, dish_id, content, images(JSON), like_count | FK → user |
| post_like | 帖子点赞 | user_id, post_id | FK → user, post; UNIQUE(user, post) |
| post_comment | 帖子评论 | post_id, user_id, content | FK → post, user |
| checkin | 打卡记录 | user_id, meal_type, dish_ids, checkin_date | FK → user |

## 设计亮点

- **时区处理**：`beijing_now()` 函数使用 `datetime.now(timezone(timedelta(hours=8)))`，不依赖服务器本地时区，确保餐次提示、打卡日期、人流量判断在任何时区服务器上都正确显示北京时间
- **品类图片三级回退**：`get_dish_image()` → 菜品自有图片 → 品类代表图（文件存在校验）→ 占位图标；子品类无图时自动回退到父品类
- **品类自动填充**：`fill_categories.py` 根据档口名关键词匹配 98 条规则，批量标记菜品品类
- **模块化路由**：认证、主路由、社区、打卡、管理后台各自独立的 Blueprint 模块，app.py 作为应用工厂负责组装
- **CSRF 防护**：对所有 POST/PUT/DELETE/PATCH 请求校验 `_csrf_token`，防止跨站请求伪造
- **响应式布局**：PC 端顶部导航栏 + 移动端底部 TabBar，适配校园使用场景
- **轻量化部署**：零前端框架依赖，SQLite 单文件数据库，适合 PythonAnywhere 免费版

## 管理员信息

- 用户名：`test` / 密码：`123456`
- 上线前建议修改

## 日常运维

### 更新菜品数据
1. 填写 `data/菜品导入模板.xlsx`
2. 运行 `python import_real_data.py`
3. 上传新的 `database.db` 到 PythonAnywhere
4. Web → Reload

### 更新品类图片
1. 替换 `static/images/dishes/cat*.jpg`
2. 更新 `static/images/dishes/category_images.json`（如需调整映射）
3. 上传覆盖 → Reload

### 部署更新
1. 上传变更文件到 PythonAnywhere
2. 删除 `__pycache__`：`rm -rf ~/mysite/__pycache__`
3. 重新加载 WSGI：`touch ~/mysite/wsgi.py`
4. Web → Reload
